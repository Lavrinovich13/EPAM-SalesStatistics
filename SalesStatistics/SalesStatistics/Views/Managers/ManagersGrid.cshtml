@using GridMvc.Html
@using GridMvc.Sorting
@model IEnumerable<SalesStatistics.Models.Manager>

<div id="t1">
    @Html.Grid(Model).Named("grid").Columns(columns =>
    {
        columns.Add(c => c.LastName).Titled("Manager").Filterable(true);
        columns.Add()
                      .Encoded(false)
                      .Sanitized(false)
                      .SetWidth(30)
                      .RenderValueAs(o => Ajax.ActionLink("Edit", "EditManager", new { id = o.Id }, new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId = "t1"  }).ToHtmlString());
        columns.Add()
        .Encoded(false)
        .Sanitized(false)
        .SetWidth(30)
        .RenderValueAs(o => Html.ActionLink("Remove", "RemoveManager", new { id = o.Id }));
    }).Sortable(true).WithPaging(10)
</div>

